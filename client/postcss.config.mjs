const config = {
  plugins: ['@tailwindcss/postcss'],
  theme: [
    {
      fontFamily: {
        spaceGrotesk: ['var(--font-space-grotesk)', 'sans-serif'],
      },
    },
  ],
}

export default config
